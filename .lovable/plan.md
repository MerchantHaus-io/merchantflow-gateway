

## Pipeline List View: Inline Underwriting Preview + Enhanced UW System

### What Changes

**1. Pipeline List View — Side Panel Instead of Modal**

Currently, clicking a row in the Pipeline Overview list opens the full `OpportunityDetailModal`. Instead, clicking a row will highlight it and render an **inline detail panel to the right** showing:
- The latest underwriting report summary (score/10